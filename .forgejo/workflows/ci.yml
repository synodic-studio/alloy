name: CI
on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]
jobs:
  build-and-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: cd Alloy && swift build
      - name: Test
        run: cd Alloy && swift test
